import card from "./ui-config/card";

export default defineAppConfig({
    ui: {
        colors: {
            primary: 'blue',
            secondary: 'purple',
            neutral: 'zinc'
        },
        card
    }
})
